# =================
# Navigation
# =================
alias ..='cd ..'
alias x='exit'
alias j="z"         # https://github.com/rupa/z


alias t="tmux"

# =================
# AI
# =================
alias q="node ~/projects/nicechat/dist/index.js"
alias qco="nicechat chat claude-opus"
alias qcs="nicechat chat claude-sonnet"

mkcd() {
  mkdir $@ && cd $_
}

# ====================
# Default replacements
# ====================
alias cat='bat'
alias find='fd'
alias l='ls -lahF'
alias v='nvim'
alias y='yarn'
alias vnext='~/Downloads/nvim-osx64/bin/nvim'
alias d="docker"

# =================
# Rails
# =================
alias b='bundle'
alias rs='bin/rails server'
alias rc='bin/rails console'
alias rdm='bundle exec rake db:migrate'

# =================
# Git
# =================
alias g="git"
alias gs='git s'
alias gd='git d'
alias g-='git checkout -'
alias p='git push'
alias c='git commit'
alias cm='git commit -m'
alias ca='git commit --amend'
alias re='git checkout $(git recent | fzf)' # recent branches, see .gitconfig aliases

# =================
# Nicechat
# =================
alias sonnet="q chat sonnet"
alias grokf="q chat grokf"
alias g3="q chat g3"

# view commit, eg: vc 1234567
vc() {
	if [ -z "$1" ]; then
		echo "Usage: vc <commit-hash>"
		return 1
	fi

	local commit_hash="$1"
	local repo_url
	repo_url="$(git config --get remote.origin.url)"

	if [ -z "$repo_url" ]; then
		echo "Not a git repository, or no remote.origin.url is set"
		return 1
	fi

	repo_url="${repo_url%.git}"

	case "$repo_url" in
		git@*:* )
			repo_url="${repo_url#git@}"
			repo_url="https://${repo_url/:/\/}"
			repo_url="${repo_url//\\\///}"
			;;
	esac

	local commit_url="${repo_url}/commit/${commit_hash}"
	echo "Opening $commit_url"
	open "$commit_url"
}

# =================
# Personal
# =================

cn() {
  open "https://app.candlapp.com/api/authors/fbn?name=$*"
}

# =================
# mt
# =================
alias mt="~/projects/mt/scripts/mt.sh"
alias mts="~/projects/mt/scripts/mt_sync.sh"
